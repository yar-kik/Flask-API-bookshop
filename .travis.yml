sudo: required
services:
  - docker
language: python
python:
  - "3.8"
before_install:
  - cd backend
install:
  - pip install -r requirements.txt
  - pip install coveralls
script:
  - coverage run --source="auth/","library/" -m unittest
  - cd ../ && pylint backend --ignore=migrations --rcfile=backend/.pylintrc
after_script:
  - coveralls
#after_success:
#  - sh .travis/deploy_dockerhub.sh
